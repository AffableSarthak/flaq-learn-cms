const Drawer = {
  variants: {
    mainsidebar: {
      dialog: {
        pointerEvents: "auto",
        bg: "#161B20",
        color: "white",
      },
      dialogBody: {
        pointerEvents: "none",
      },
    },
  },
};

export default Drawer;
